chai = require 'chai'
expect = chai.expect

{interpolate, getPercent} = require '../lib/utils'

describe 'Utils', ->
  describe 'interpolate', ->


assert()

tests =
  interpolate: ->
    assert

  getPercent: ->
    assert()



for name, test of tests
  test()
